machine:
  node:
    version: 5.11.1
  environment:
    HOSTNAME: localhost
    PORT: 8443
dependencies:
  override:
    - npm install
    - npm run dev-generatesslcerts
deployment:
  staging:
    branch: master
    heroku:
      appname: egeria-collector
